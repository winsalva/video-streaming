<%= if @conn.assigns.current_user do %>
<p>Logged in as: <%= @conn.assigns.current_user.name %></p>
<% end %>

<%= if @uploads == [] do %>
<div>
  <p>No available uploads yet.</p>
</div>
<% else %>
<%= for upload <- @uploads do %>
    <div>
      <p>Title: <%= upload.title %></p>
    </div>
    <% end %>
    <% end %>
    
